import React from 'react'
import GlobalPage from '../../../../custom/GlobalPage';

const Kisiler = ({ match }) => {
    return <GlobalPage
        heading='menu.is-katalogu.organizasyon-mimarisi.kisiler'
        match={match}
        subCatalogId={1016} />
};

export default Kisiler;