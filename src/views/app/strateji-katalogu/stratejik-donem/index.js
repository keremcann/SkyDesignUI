import React from 'react'
import GlobalPage from '../../../custom/GlobalPage';

const StratejikDonem = ({ match }) => {
    return <GlobalPage
        heading='menu.strateji-katalogu.stratejik-donem'
        match={match}
        subCatalogId={1030} />
};

export default StratejikDonem;