import React from 'react'
import GlobalPage from '../../../../custom/GlobalPage';

const IPAC = ({ match }) => {
    return <GlobalPage
        heading={'menu.teknoloji-katalogu.altyapi-teknoloji-varliklari.ipac'}
        match={match}
        subCatalogId={1006} />
};

export default IPAC;