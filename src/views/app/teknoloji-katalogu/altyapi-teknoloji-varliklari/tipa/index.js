import React from 'react'
import GlobalPage from '../../../../custom/GlobalPage';

const TIPA = ({ match }) => {
    return <GlobalPage heading={'menu.sertifikalar'} match={match} subCatalogId={1007} />
};

export default TIPA;