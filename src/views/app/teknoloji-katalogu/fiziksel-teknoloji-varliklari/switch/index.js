import React from 'react'
import GlobalPage from '../../../../custom/GlobalPage';

const Switch = ({ match }) => {
    return <GlobalPage
        heading='menu.teknoloji-katalogu.fiziksel-teknoloji-varliklari.switch'
        match={match}
        subCatalogId={4} />
};

export default Switch;