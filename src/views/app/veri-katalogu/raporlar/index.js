import React from 'react'
import GlobalPage from '../../../custom/GlobalPage';

const Raporlar = ({ match }) => {
    return <GlobalPage
        heading='menu.veri-katalogu.raporlar'
        match={match}
        subCatalogId={1053} />
};

export default Raporlar;